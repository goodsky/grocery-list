import { Typography } from '@mui/material'

const Register = () => {
    return (
        <div>
            <Typography variant="h2">Register</Typography>
        </div>
    )
}

export default Register
